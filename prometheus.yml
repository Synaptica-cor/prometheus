global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'synaptica'
    scheme: 'https'  # Вот это поле нужно явно указать для HTTPS
    metrics_path: '/metrics'
    static_configs:
      - targets:
          - 'synaptica-production.up.railway.app'
